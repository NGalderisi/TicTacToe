import { Computerdifficulty } from './computerdifficulty';

describe('Computerdifficulty', () => {
  it('should create an instance', () => {
    expect(new Computerdifficulty()).toBeTruthy();
  });
});
